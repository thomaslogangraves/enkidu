const parser = require('./pull_parse')
const compute = require('./compute');


const fileName = "HR.csv"
console.log(parser.parseTool());
// const computation = compute([1,2,3,4,5,6]);

// console.log("computation", computation);
